# Limitations and unresolved items

## Capture limitation
Our GitHub Actions runner successfully installed `yt-dlp` and FFmpeg, but YouTube rejected direct extraction with a sign-in/bot-verification challenge. Therefore:
- transcript: captured;
- oEmbed metadata: captured;
- original video bytes: not captured;
- direct scene frames: not captured.

A third-party timestamped storyboard/transcript corroborates the visible chapter structure, but we have not independently inspected every on-screen prompt/config pixel.

## Unresolved dependency 1 — mobile repo
Jack says he uses/shares a proven mobile optimization repo, but its identity is not recoverable from the captured transcript/resource list. **Do not guess.**

## Unresolved dependency 2 — Claude SEO repo
Jack demonstrates a well-reviewed Claude SEO GitHub repo but the exact repository name/URL is not visible in our current evidence. Candidate repos found by web search must remain candidates, not source facts, until the source frame/link resolves it.

## Design Loop internals
The current video tells the viewer to install the Design Loop skill but doesn't show its whole instruction file. The brief/system/craft critic model comes from a separate Jack Roberts tutorial and is therefore cross-source reconstruction.

## Verification status
`RECONSTRUCTED`, not `VERIFIED`. Upgrade only after direct visual/source review resolves the critical missing on-screen details.
